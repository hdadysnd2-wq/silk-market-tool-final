# خطة التنفيذ المحدّثة — منصة سِلك

> **المصدر:** تقاطع تدقيق `docs/AUDIT_STATUS.md` (إيداع `a7c36bb`)
> مع وثيقة الرؤية `docs/VISION.md`.
>
> **تحل محل** ترتيب التنفيذ الوارد بترويسة وثيقة الرؤية
> (الذي افترض بنية غير موجودة: 21 وكيلاً، /deepen، synthesis...).
>
> **قاعدة عامة:** كل موجة PR مستقل، واختبارات الدخان الـ24
> تبقى ناجحة بعد كل موجة، وكل موجة تضيف اختباراتها.

---

## الموجة ٠ — إيقاف النزيف (أمنية، قبل أي شيء)

> لا نشر عام بمفاتيح حقيقية قبل اكتمالها.

1. **مصادقة `POST /analyze`** (`api.py:129-143`): مفتاح API من
   متغير بيئة، رفض 401 بدونه — الفحص قبل تشغيل أي وكيل.
2. **سقف تكلفة يومي:** عدّاد استدعاءات الطبقات المدفوعة في
   SQLite، رفض 429 عند التجاوز، الحد من متغير بيئة.
3. **عزل الحقن في `silk_ai_judge.py:64-94`:** كل نص خارجي
   (بحث ويب، أسماء شركات Volza/explee) يُغلّف بوسوم عزل مع
   تعليمة "بيانات لا أوامر" + اختبار دخان بنص حقن هجومي.
4. تقييد CORS من `"*"` (`api.py:79-82`) بمتغير بيئة.

**قبول:** طلب بلا مفتاح = 401؛ تجاوز السقف = 429؛ اختبار الحقن
يمر؛ الـ24 القديمة تمر.

---

## الموجة ١ — نظافة رخيصة (أيام قليلة، بلا قرارات معمارية)

1. **الأغلفة التسعة الصامتة** (`silk_engine.py:157-268`):
   استثناء الغلاف → `DataPoint(None, note=السبب)` بدل `[]`/`None`.
2. **إلغاء افتراض WATCH** (`silk_ai_judge.py:102-104`): فشل
   تفسير JSON → `verdict: null` + ملاحظة، والواجهة تعرض
   "تعذّر الحكم" لا وسماً مختلقاً.
3. **عمودا `outcome` + `outcome_date`** في `analyses`
   (`silk_storage.py:33-38`) + نقطة `PATCH /analyses/{id}/outcome`.
4. **مجلد `samples/`** بنموذج JSON حقيقي محفوظ + **زر "حمّل
   JSON"** باللوحة (البيانات موجودة، زر فقط).
5. **مواءمة التوثيق:** README «11 طبقة» لا 9، عدّ الاختبارات،
   تصحيح تعليق AI المتقادم في `.env.example:36-41`.
6. **تصحيح ترويسة `VISION.md`:** استبدال "٨٠٪ مكتملة" بإحالة:
   "الحالة الفعلية تُقرأ من `AUDIT_STATUS.md` — الوثيقة تصف
   الحالة المستهدفة".

---

## الموجة ٢ — البنية قبل التوسّع

1. **مسار `/deepen` صريح** يستوعب أعلام `with_volza/explee/
   localprice` (`api.py:88-97`) — المسار العادي يفقد قدرة تفعيل
   المدفوع نهائياً (الحصر يصير بنيوياً لا افتراضياً).
2. **`BaseAgent` الفارض** (تصنيف `PAID`، حارس `/deepen`،
   استثناء → `DataPoint(None, note)` تلقائياً) — يُبنى الآن لأن
   الموجة ٣ ستضيف وكلاء جدداً عليه من يومهم الأول.
3. هجرة الوكلاء الـ11 القائمين إليه تدريجياً (وكيل مع كل PR لاحق،
   لا هجرة جماعية).

---

## الموجة ٣ — الوكلاء الناقصون (انتقائياً لا استنساخاً)

> **قرار مطلوب من المالك:** لا نبني العشرة الغائبين كلهم —
> نبني فقط ما يغذّي محرّك التقاطع، بهذا الترتيب:

| الأولوية | الوكيل | لماذا |
|---|---|---|
| ١ | منافسون مُسمّون (competitors) | خيط ١ بالمحرّك يستحيل بدونه — `CompetitionAgent` الحالي دول لا شركات |
| ٢ | قنوات التوزيع (distribution_channels) | خيط أبواب الدخول |
| ٣ | مستوردون (importers) | خيط الأبواب + ترقية explee لاحقاً |
| ٤ | الاشتراطات (requirements) | القسم الخامس بالرؤية — يبدأ بالخليج ثم أوروبا |
| مؤجّل | cultural / exhibitions / religion / currency / ecommerce / factories | لا يغذّي المحرّك مباشرة — يُبنى عند الحاجة الفعلية |

كل وكيل جديد: يرث `BaseAgent`، مجاني أو محصور `/deepen`،
باختبار دخان هيرمتيكي من يومه الأول.

---

## الموجة ٤ — محرّك التقاطع (القسم الأول من الرؤية كما هو)

1. `product_card` في نموذج الإدخال (`api.py:84-98`) — اختياري.
2. `correlation.py` بقاعدته الصارمة (صفر API خارجي) — النواة
   موجودة كبذرة: `compare_own_price` (`silk_localprice_agent.py:145`)
   تتوسع لخيط الجدوى الكامل (تكلفة + جمارك WITS + شحن معلن).
3. توحيد الحكمَين (`JuryCommittee` + `ai_verdict`) في synthesis
   ثنائية المرحلة — **هنا** يتحقق شرط حذف الازدواجية (٩.٣).
4. قسم "موقعك التنافسي" بالمخرجات = فرصة بناء **القالب الموحّد**
   (١٠.١) بدل المسارات الثلاثة المنفصلة الحالية.

---

## الموجة ٥ — ما بعد المحرّك (ترتيب الرؤية يعود سارياً)

اكتشاف الفرص المعكوس (القسم ٤) ← طبقة الامتثال الكاملة
(القسم ٥: الخليج ثم أوروبا) ← طبقة التمويل التجاري (١٢.٧ —
مؤجّلة بقرار المالك) ← تقرير Word/المختصر على القالب الموحّد.

---

## القرارات — محسومة من المالك (2026-07-02)

1. **SQLite أم Postgres؟ → SQLite تبقى.** لا هجرة؛ الهجرة قرار
   مؤجل حتى ظهور حاجة فعلية (تعدد مستخدمين متزامن/خادم منفصل).
   أي ذكر لـ Postgres بالوثائق يُقرأ: "SQLite (الهجرة قرار مؤجل
   عند الحاجة)".
2. **حدود الموجة ٣ → القائمة الانتقائية (٤ وكلاء فقط).**
   الباقي مؤجل حتى الحاجة الفعلية.
3. **توقيت تحويل PR ‏#12 إلى ready → بعد تصحيح الترويسة**
   (نُفّذ ضمن المرحلة أ من تفويض التنفيذ الشامل).
4. **قسم التمويل التجاري (١٢.٧ بالرؤية) → مؤجل** — لا يُنفّذ
   منه شيء في الموجات الحالية.
5. **الطوابير الخلفية (RQ/Redis) → لا تُنفّذ نهائياً.** قرار مالك
   المشروع: لا طوابير خلفية (RQ/Redis) — أداة داخلية، التزامن
   بالخيوط يكفي؛ يُعاد النقاش فقط عند تعدد مستخدمين بحمل حقيقي.
   (تدقيق `AUDIT_STATUS.md` أثبت أنها لم تُخطَّط في الموجات ٠-٥
   ولا ذكر لها في الكود/التوثيق/تاريخ git — تُوثَّق هنا كي لا تظهر
   في أي تدقيق لاحق كغائب صامت.)

---

## مبادرة البحث العميق (موجات ١-١١) — إضافية، سجلّها منفصل

> هذه الخطة (موجات ٠-٥ أعلاه) لا تذكر مبادرة "البحث العميق"
> (Silk Deep Market Research: ١٢ وكيل كلود + محلل شامل + كاتب/
> مراجع + `POST /research`) لأنها طبقة **إضافية بالكامل** بُنيت
> فوق النظام القائم بقرار مالك مباشر أثناء البناء — لا تُعدِّل أي
> من موجات ٠-٥ أعلاه ولا تعتمد عليها معمارياً (`silk_engine.analyze`/
> `/analyze` يعملان بلا تغيير). سجلّ قراراتها الكامل، حادثة تلو
> حادثة، في **`docs/DEEP_RESEARCH_DECISIONS.md`** — لا تُكرَّر هنا.

**الحالة: موجات ١-١١ منفَّذة ومدموجة بالكامل.**

| الموجة | الموضوع | الحالة |
|---|---|---|
| ١-٤ (V5) | زمن تشغيل أدوات كلود، ١٢ بعثة، المحلل الشامل، كاتب/مراجع، `POST /research` | مدموجة |
| ٥-٦ (V5) | أداة تقييم (`silk_evals.py`)، تتبّع/تنقيح البحث | مدموجة |
| ٧ | أول تشغيلة حية — تشخيص حادثة `/analyze` مقابل `/research` + بوابة ما قبل التشغيل | مدموجة |
| ٨ | أول جولة تنقيح ببيانات حية (هولندا/تمور) — سياج JSON، جولة إنهاء قسرية، بنية التقرير | مدموجة |
| ٩ | تقرير قابل للبيع — شارات أدلة بدل ثقة خام، جداول حقيقية، حجّة (أطروحة/خارطة طريق/خلاصات) | مدموجة |
| ١٠ | حلقة جودة ذاتية الإغلاق (`silk_quality_gate.py`) + بنية علمية دولية بأحد عشر قسماً | مدموجة |
| ١١ | هوية سِلك البصرية (`config/branding.yaml`) + تصليب وكيلَي المنافسين والمخاطر + جاهزية الإصدار | مدموجة |

**فجوة معلنة مستمرة عبر كل الموجات**: `evals/golden_cases.json`
فارغ — بناء حالة ذهبية حقيقية يتطلب رقماً مُتحقَّقاً يدوياً من مصدر
رسمي حي (Comtrade مباشرة)، وهذه البيئة بلا وصول شبكي لأي مصدر بيانات
حقيقي طوال كل الموجات (نفس القيد الموثَّق في كل تحديث لهذا الملف).
البنية والمخطط جاهزان بالكامل ومُختبَران؛ راجع `docs/DEEP_RESEARCH_
DECISIONS.md` قسم "خطوات أول جلسة حية" للمسار الكامل عند توفر بيئة
متصلة، و`docs/RELEASE_NOTES_v1.md` للحالة التشغيلية الكاملة والحدود
المعلنة.
