import { SendingEmailManager } from "@/components/SendingEmailManager";

// Settings › Sending email — manage the factory's connected sending mailboxes.
export default function SendingEmailSettingsPage() {
  return <SendingEmailManager />;
}
