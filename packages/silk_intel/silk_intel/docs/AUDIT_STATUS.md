# تقرير تدقيق حالة المستودع — منصة سِلك

> **تنبيه تاريخي (2026-07-05):** هذه الوثيقة لقطة زمنية عند الإيداع
> `a7c36bb` (2026-07-02) **قبل** دمج الموجات 0–5c. الموجات المدموجة
> نفّذت معظم ما ورد أدناه بوصفه "غير موجود" (مثل `correlation.py`
> و`/deepen` و`BaseAgent` و`samples/` وأعمدة `outcome`). للحالة الراهنة
> اقرأ `CLAUDE.md` والكود نفسه — لا هذه الوثيقة.
> *Historical note (2026-07-05): this document is a point-in-time
> snapshot at commit `a7c36bb`; waves 0–5c have since been merged and
> implemented most items listed here as "not found". For the current
> state, read `CLAUDE.md` and the code itself.*

> **نوع المهمة:** تدقيق قراءة فقط — لم يُعدَّل أي كود أو ملف قائم.
> **نطاق الفحص:** كامل ملفات المستودع على فرع `main`
> (آخر إيداع وقت التدقيق: `a7c36bb`).
> **تاريخ التدقيق:** 2026-07-02.
> **القاعدة الملتزَم بها:** كل ادعاء مُسنَد إلى ملف ورقم سطر.
> ما لم يوجد في الكود قيل عنه صراحةً: **غير موجود**.
> **مرجع المقارنة:** وثيقة الرؤية المعمارية `docs/VISION.md`
> (المقدَّمة في PR ‏#12، غير مدموجة في `main` وقت التدقيق).

---

## الخلاصة الأولى (قبل التفاصيل)

المستودع الفعلي **أصغر بكثير من الصورة التي تفترضها وثيقة الرؤية**:

- الوكلاء الموجودون فعلاً: **11 وكيلاً** (لا 21).
- لا يوجد مسار `/deepen`، ولا `correlation.py`، ولا `silk_synthesis`،
  ولا طبقة باسم `jury.ai` كملف مستقل، ولا Postgres (التخزين SQLite)،
  ولا مجلد `samples/`، ولا سقف تكلفة، ولا مصادقة على الـ API.
- المبدأ التأسيسي ("لا اختلاق") **مطبَّق فعلاً وباتساق عالٍ** داخل
  الوكلاء وطبقة البيانات، وتحرسه اختبارات دخان هيرمتيكية
  (24 اختباراً في `tests/test_smoke.py`).

---

## ١) جرد الوكلاء

### ١.١ العدد الفعلي

تعريف "الوكيل" المعتمد في الكود: فئة ترث `Agent` (الفئة الأساس
المجردة في `silk_agents.py:35-44`) وتنفّذ `run()` وتعيد `AgentReport`
(`silk_agents.py:25-32`).

**العدد الفعلي: 11 وكيلاً — لا 21.** الرقم 21 الوارد في وثيقة الرؤية
**غير موجود** في الكود بأي صورة.

| # | الوكيل | الملف : السطر | المصدر |
|---|---|---|---|
| 1 | `TradeFlowAgent` | `silk_agents.py:52` | UN Comtrade |
| 2 | `EconomicAgent` | `silk_agents.py:80` | World Bank |
| 3 | `CompetitionAgent` | `silk_agents.py:101` | UN Comtrade |
| 4 | `TrendsAgent` | `silk_trends_agent.py:86` | Google Trends (pytrends) |
| 5 | `TariffsAgent` | `silk_tariffs_agent.py:120` | World Bank WITS |
| 6 | `FaostatAgent` | `silk_faostat_agent.py:124` | FAOSTAT |
| 7 | `MapsAgent` | `silk_maps_agent.py:87` | Google Places |
| 8 | `WebSearchAgent` | `silk_websearch_agent.py:84` | Serper.dev |
| 9 | `LocalPriceAgent` | `silk_localprice_agent.py:193` | SerpApi (Google Shopping) |
| 10 | `VolzaAgent` | `silk_volza_agent.py:121` | Volza (بوالص شحن) |
| 11 | `ExpleeAgent` | `silk_explee_agent.py:147` | explee (B2B) |

مكوّنات تحليلية أخرى **ليست وكلاء** (لا ترث `Agent`):
`ResearchManager` (`silk_agents.py:123`)، `JuryCommittee`
(`silk_agents.py:141`)، طبقة كلود `silk_ai_judge.py` (دوال وحدة،
ليست فئة وكيل)، مصنّف HS (`silk_hs_resolver.py`)، المُرتّب
(`silk_market_ranker.py`).

وكلاء تفترضها وثيقة الرؤية وهي **غير موجودة** بالكود إطلاقاً:
`cultural`، `business_culture`، `exhibitions`، `distribution_channels`،
`ecommerce_landscape`، `religion_agent`، `currency_risk_agent`،
`factories_agent`، `importers_agent`، ووكيل اشتراطات/امتثال.
(تم التحقق بالبحث النصي الشامل في `*.py` — صفر نتائج.)

### ١.٢ تصنيف مجاني/مدفوع

التصنيف الرسمي الوحيد في الكود هو قائمة `GET /sources`
(`api.py:152-164`):

| الوكيل | التصنيف (حسب `api.py`) | بوابة المفتاح |
|---|---|---|
| TradeFlow / Economic / Competition | مجاني (`api.py:153-154`) | بلا مفتاح (مفتاح Comtrade اختياري، `silk_data_layer.py:50`) |
| TrendsAgent | مجاني (`api.py:157`) | بلا مفتاح (يتطلب حزمة pytrends فقط، `silk_trends_agent.py:32-36`) |
| TariffsAgent | مجاني (`api.py:156`) | بلا مفتاح |
| FaostatAgent | مجاني (`api.py:155`) | بلا مفتاح (قد يرد 401/403، `silk_faostat_agent.py:76-80`) |
| MapsAgent | مجاني بمفتاح (`api.py:158`) | `GOOGLE_MAPS_API_KEY` (`silk_maps_agent.py:34-38`) |
| WebSearchAgent | مجاني بمفتاح (`api.py:159`) | `SEARCH_API_KEY` (`silk_websearch_agent.py:48-52`) |
| LocalPriceAgent | **مدفوع** (`api.py:160`) | `LOCALPRICE_API_KEY` (`silk_localprice_agent.py:101-106`) |
| VolzaAgent | **مدفوع** (`api.py:161`) | `VOLZA_API_KEY` (`silk_volza_agent.py:47-50`) |
| ExpleeAgent | **مدفوع** (`api.py:162`) | `EXPLEE_API_KEY` (`silk_explee_agent.py:49-54`) |
| طبقة كلود (ليست وكيلاً) | `ai` (`api.py:163`) | `ANTHROPIC_API_KEY` (`silk_ai_judge.py:39-41`) |

ملاحظة توثيقية: جدول README يذكر «الطبقات التسع» (`README.md:48`،
الجدول `README.md:57-67`) بينما `api.py:152-164` يسرد **11 طبقة**
(أُضيفت Local retail وClaude) — تعارض توثيقي بسيط.

### ١.٣ هل المدفوع محصور داخل `/deepen`؟

**لا — لأن `/deepen` نفسه غير موجود** (انظر القسم ٢). الحصر الفعلي
القائم طبقتان:

1. **أعلام تفعيل اختيارية، مطفأة افتراضياً** في نموذج الطلب:
   `with_volza: bool = False` (`api.py:95`)،
   `with_explee: bool = False` (`api.py:96`)،
   `with_localprice: bool = False` (`api.py:93`) — والمحرّك لا يستدعي
   الوكيل إلا عند رفع العلم (`silk_engine.py:104-109`).
2. **بوابة مفتاح داخل الوكيل نفسه:** بلا مفتاح لا يُجرى أي نداء شبكة
   ويُعاد `DataPoint(None, ..., "requires ...")`
   (`silk_volza_agent.py:47-50`، `silk_explee_agent.py:49-54`،
   `silk_localprice_agent.py:101-106`).

**الثغرة المفتوحة (مخالفة بالاسم والسطر):** نقطة `POST /analyze`
(`api.py:129-143`) **عامة بلا مصادقة وبلا سقف تكلفة** — أي زائر
مجهول يستطيع إرسال `with_volza=true, with_explee=true, with_ai=true`،
وإذا كانت المفاتيح المدفوعة مضبوطة في بيئة الخادم فسيستهلك رصيداً
مدفوعاً (استدعاءات Volza/explee/SerpApi/Anthropic) بلا أي حارس.
الحماية الحالية "علم + مفتاح" تمنع الاستدعاء **العرضي** لا
**الاستغلالي**. (تفصيل المصادقة والسقف في القسم ٥.)

### ١.٤ هل الفشل مرئي أم صامت؟

**داخل الوكلاء أنفسهم: مرئي باتساق ممتاز.** كل مسار فشل يعيد
`DataPoint(value=None, confidence=0.0, note=السبب)` أو
`AgentReport(failed=True, summary=...)`:

- TradeFlow: `silk_agents.py:65-68` — Economic: `silk_agents.py:89-91`
  — Competition: `silk_agents.py:112-117`
- Trends: `silk_trends_agent.py:34-36, 54-57` — Tariffs:
  `silk_tariffs_agent.py:56-65` — FAOSTAT:
  `silk_faostat_agent.py:76-96` — Maps: `silk_maps_agent.py:36-38, 56-64`
  — WebSearch: `silk_websearch_agent.py:50-52, 64-67` — LocalPrice:
  `silk_localprice_agent.py:101-106, 125-128` — Volza:
  `silk_volza_agent.py:49-50, 70-74` — Explee:
  `silk_explee_agent.py:51-54, 75-78`
- استثناء غير متوقع من وكيل أساسي: `ResearchManager.distribute` يلتقطه
  ويعيد تقريراً فاشلاً بملاحظة (`silk_agents.py:133-137`) — لا انهيار
  ولا صمت.
- الاختبارات تفرض ذلك هيرمتيكياً (قطع الشبكة):
  `tests/test_smoke.py:153-168` وغيرها.

**المخالفون بالاسم — الفشل شبه الصامت (يُسجَّل في اللوق فقط ولا
يظهر في بيانات المخرجات):** أغلفة الإثراء في المحرّك، عند استثناء
غير متوقع من الوكيل نفسه، تضع قيمة فارغة **بلا ملاحظة سبب** بدل
`DataPoint(None, note=...)`:

| الغلاف | السطر | ما يضعه عند الاستثناء |
|---|---|---|
| `_enrich_trends` | `silk_engine.py:165-167` | `row["trends"] = []` |
| `_enrich_tariffs` | `silk_engine.py:179-181` | `row["tariff"] = None` |
| `_enrich_faostat` | `silk_engine.py:193-195` | `row["faostat"] = None` |
| `_enrich_maps` | `silk_engine.py:207-209` | `row["maps"] = []` |
| `_enrich_localprice` | `silk_engine.py:226-230` | `row["localprice"] = []` |
| `_enrich_volza` | `silk_engine.py:242-244` | `row["volza"] = []` |
| `_enrich_explee` | `silk_engine.py:255-257` | `row["explee"] = []` |
| `_websearch` | `silk_engine.py:266-268` | `[]` |
| `_ai_report` | `silk_engine.py:126-128, 152-154` | مفتاح `report` يُحذف من النتيجة كلياً |

المستهلك النهائي لا يميّز بين "الطبقة لم تجد شيئاً" و"الطبقة انفجرت
باستثناء" — كلاهما `[]`/`None`. أثر عملي محدود (الوكلاء مصمَّمون
ألا يرموا استثناءً أصلاً)، لكنه انحراف حقيقي عن قاعدة «الفشل
المرئي بملاحظة» مقارنةً بصرامة الوكلاء الداخلية.

**مخالفة أدق — قيمة افتراضية غير مشتقة من البيانات:** عند فشل
تفسير ردّ كلود كـ JSON، يُعاد الحكم بقيمة افتراضية
`"verdict": "WATCH"` (`silk_ai_judge.py:102` و `:104`) — وسم حكم
لم يصدر عن النموذج ولا عن البيانات. الثقة تبقى `None` (لا تُختلق)،
لكن وسم WATCH الافتراضي يُعرض في الواجهة كأنه حكم
(`web/index.html:464-472`).

---

## ٢) المسارات والطبقات

### ٢.١ `/analyze` مقابل `/deepen`

- `POST /analyze`: **موجود** (`api.py:129-143`) — المسار الوحيد للتحليل.
- `/deepen`: **غير موجود** — لا في `api.py` (النقاط الكاملة:
  `/health`، `/resolve/{name}`، `/index`، `/analyze`، `/sources`،
  `/analyses`، `/analyses/{id}` — الأسطر 104-183) ولا في أي ملف
  (بحث نصي شامل: صفر نتائج خارج `docs/VISION.md`).
- **المكافئ الفعلي للتعميق:** أعلام `with_*` داخل جسم `/analyze`
  نفسه (`api.py:88-97`)، تُفعَّل يدوياً من الواجهة حسب المفاتيح
  المحفوظة محلياً في المتصفح (`web/index.html:352-357`؛ ملاحظة:
  «مفاتيح» الواجهة مجرد مفاتيح تشغيل في `localStorage` —
  `web/index.html:267` — ولا تُرسَل للخادم أبداً؛ المفاتيح الحقيقية
  في بيئة الخادم فقط).
- بذرة واجهة فقط: زر «دراسة عميقة · Volza/explee» في اللوحة
  (`web/index.html:449`) لا يشغّل شيئاً — يفتح شاشة الإعدادات
  (`web/index.html:459`).

### ٢.٢ `correlation.py` أو بذرة محرّك تقاطع

الملف: **غير موجود**. البذرة الوحيدة القريبة من روح «خيط الجدوى»
هي `compare_own_price()` (`silk_localprice_agent.py:145-190`):
تقارن سعر المستخدم (`own_price`، مدخل عبر `api.py:94`) بالقوائم
المرصودة وتعيد موقعه المئوي — وهي مقارنة تموضع سعري، **ليست**
حساب هامش ضد منافس مُسمّى (لا تكلفة، لا رسوم جمركية، لا شحن).
بطاقة المنتج (`product_card`) بحقولها الأربعة: **غير موجودة**
في نموذج الإدخال (`api.py:84-98`).

### ٢.٣ طبقة `jury.ai`

لا يوجد ملف بهذا الاسم: **غير موجود**. لكن **ازدواجية الحكم التي
تصفها الرؤية قائمة فعلاً** بمكوّنين منفصلين:

1. `JuryCommittee.evaluate` — حكم حتمي (بلا AI) يحسب verdict/ثقة/فجوات
   (`silk_agents.py:141-176`)، يُرفق لكل سوق أعلى في
   `row["jury"]` (`silk_engine.py:91`).
2. حكم كلود `ai_verdict` (`silk_ai_judge.py:83-109`) يُرفق **داخل**
   حكم اللجنة في `row["jury"]["ai"]` (`silk_engine.py:142`) عند
   `with_ai=True`.

أي أن «طبقتي الحكم المتوازيتين» المطلوب توحيدهما لاحقاً (قسم ٩.٣
من الرؤية) موجودتان اليوم في `silk_agents.py` + `silk_ai_judge.py`.

### ٢.٤ `silk_synthesis`: مرحلة أم مرحلتان؟ وأين حماية الحقن؟

- ملف/وحدة `silk_synthesis`: **غير موجود**.
- الموجود الفعلي: نداءان منفصلان أحاديا المرحلة في `silk_ai_judge.py`:
  `ai_verdict` لكل سوق (`silk_ai_judge.py:83-109`) و`ai_report`
  للنتيجة كلها (`silk_ai_judge.py:112-137`). **ليسا** مرحلتَي
  synthesis متسلسلتين — كل منهما برومبت مستقل بنداء واحد.
- **حماية الحقن: غير موجودة.** نتائج الوكلاء — ومنها نصوص قادمة
  من الويب المفتوح (عناوين ومقتطفات بحث `silk_websearch_agent.py:75-80`،
  أسماء شركات من Volza/explee `silk_volza_agent.py:110-117`
  و`silk_explee_agent.py:108-121`) — تُحقَن مباشرةً في نص برومبت
  المستخدم عبر `_facts()` (`silk_ai_judge.py:64-80`) ثم
  (`silk_ai_judge.py:89-94`) وفي `ai_report` (`silk_ai_judge.py:120-136`)
  **بلا أي عزل أو فواصل أو تعقيم**. الدفاع الوحيد هو تعليمات المبدأ
  في الـ system prompt (`silk_ai_judge.py:24-29`) — دفاع سلوكي لا
  بنيوي. آلية «العزل في raw_findings» التي تعتبرها الرؤية قائمةً:
  **غير موجودة**.

---

## ٣) المخرجات

### ٣.١ المخرجات المولّدة فعلاً

| المخرج | موجود؟ | الدليل |
|---|---|---|
| لوحة ويب (Dashboard) | ✅ | `web/index.html` (660 سطراً)، تُقدَّم على `/` من نفس الخدمة (`api.py:185-190`) |
| JSON خام | ✅ جزئياً | استجابة `/analyze` و`/analyses/{id}` هي JSON الكامل بطبيعتها (`api.py:129-143, 176-183`) — لكن **لا زر «حمّل JSON» في اللوحة** (الموجود زر CSV فقط) |
| تصدير CSV من اللوحة | ✅ | `exportCsv` — `web/index.html:560-565` (توليد client-side) |
| نص كلود (تقرير مكتوب) | ✅ اختياري | يُخزَّن في `result["report"]` (`silk_engine.py:125-128`) ويُعرض overlay (`web/index.html:539-556`) |
| ملخص طرفية (terminal) | ✅ | `format_result` — `silk_engine.py:305-334` |
| واجهة Streamlit | ✅ اختيارية | `app.py` (حزمة اختيارية، `requirements.txt:6-7`) |
| **تقرير كامل (Word/docx)** | ❌ **غير موجود** | لا مكتبة docx في `requirements.txt` ولا كود توليد مستندات |
| **تقرير مختصر (صفحة واحدة)** | ❌ **غير موجود** | لا يوجد |
| **PPTX/PDF** | ❌ **غير موجود** | لا يوجد |

### ٣.٢ قالب مشترك أم مسارات منفصلة؟

**مسارات منفصلة بالكامل — لا قالب مشترك.** ثلاث طبقات عرض مستقلة
تعيد كلٌّ منها بناء النموذج من JSON الخام:

1. اللوحة: `mapResult()` في JS (`web/index.html:365` وما بعده)
2. Streamlit: `_markets_table` / `_render_market` (`app.py:24-50`)
3. الطرفية: `format_result` (`silk_engine.py:305-334`)

وهذا تحديداً نمط «أربعة مسارات لنفس الخطأ» الذي يحذّر منه قسم ١٠.١
من الرؤية — قائم اليوم بثلاثة مسارات.

### ٣.٣ مجلد `samples/`

**غير موجود** (تحقق مباشر بـ `ls` — لا مجلد بهذا الاسم في الجذر).
قاعدة «نموذج فعلي محفوظ بالمستودع لكل مخرج» (١٠.٦ من الرؤية):
غير مطبّقة.

---

## ٤) قاعدة البيانات

### ٤.١ المحرّك

**SQLite، لا Postgres.** `import sqlite3` (`silk_storage.py:13`)،
الملف الافتراضي `data/silk.db` (`silk_storage.py:17`)، وهو متجاهَل
في git (`silk_storage.py:5`). أي ذكر لـ Postgres في الكود:
**غير موجود** (بحث شامل: صفر نتائج).

### ٤.٢ الجداول والأعمدة (من `init_db`، `silk_storage.py:30-45`)

**جدول `analyses`** (`silk_storage.py:33-38`):

| العمود | النوع |
|---|---|
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `product` | TEXT |
| `hs_code` | TEXT |
| `year` | INTEGER |
| `created_at` | TEXT |
| `preliminary` | INTEGER |
| `json_blob` | TEXT (النتيجة الكاملة JSON) |

**جدول `market_scores`** (`silk_storage.py:39-45`):

| العمود | النوع |
|---|---|
| `id` | INTEGER PRIMARY KEY AUTOINCREMENT |
| `analysis_id` | INTEGER (FK → analyses.id) |
| `country` | TEXT |
| `iso3` | TEXT |
| `total_score` | REAL |
| `confidence` | REAL |

### ٤.٣ عمود `outcome` / `outcome_date`

**غير موجود** — لا في `analyses` ولا في أي جدول (الأعمدة السبعة
أعلاه حصرية، `silk_storage.py:33-38`). بند «يُضاف من اليوم الأول
وتكلفته صفر» (٩.٥ من الرؤية): غير منفّذ.

---

## ٥) البنية التشغيلية

### ٥.١ سقف التكلفة والمصادقة

- **سقف التكلفة: غير موجود** — لا عدّاد استدعاءات، لا حد إنفاق،
  لا rate limiting في `api.py` كاملاً ولا في أي وحدة.
- **المصادقة: غير موجودة** — كل نقاط `api.py` (الأسطر 104-183)
  عامة بلا أي تحقق هوية. CORS افتراضياً `"*"` (`api.py:79-82`)
  مع تعليق ينصح بتقييده إنتاجياً.
- السؤال «قبل أول وكيل أم بعده؟» غير قابل للإجابة: **الاثنان
  غائبان كلياً**، فلا يوجد ما يُقاس موضعه. قاعدة ٩.٥ من الرؤية
  («السقف والمصادقة قبل أول وكيل») غير منفّذة من أساسها.
- الأثر مركّب مع ثغرة ١.٣: `/analyze` عام + أعلام مدفوعة قابلة
  للتفعيل من أي طلب + مفاتيح خادم = استنزاف رصيد مدفوع ممكن
  لأي مجهول (إن ضُبطت المفاتيح).

### ٥.٢ `BaseAgent` أو فئة أساس

**توجد فئة أساس بسيطة — لا BaseAgent الرؤية.** الموجود:
`class Agent(ABC)` (`silk_agents.py:35-44`) بحقلَي اسم و`run()`
مجرّدة فقط. **غير موجود فيها:** تصنيف `PAID` إلزامي، حارس
`deepen_context()`، التحويل التلقائي للاستثناء إلى
`DataPoint(None, note=...)` — أي كل ما يميّز `BaseAgent` المقترح
(٩.٤ من الرؤية). أقرب سلوك للفرض البنيوي هو
`ResearchManager.distribute` الذي يحوّل استثناء الوكيل إلى تقرير
فاشل (`silk_agents.py:129-138`) — لكنه يغطي الوكلاء الثلاثة
الأساسيين فقط؛ الطبقات الاختيارية تُغلَّف يدوياً في المحرّك واحدةً
واحدة (`silk_engine.py:157-268`) وهو تكرار الالتزام اليدوي الذي
تريد الرؤية إلغاءه.

### ٥.٣ مصادر البيانات الخارجية — الموصول والمعطّل

| المصدر | الوحدة : السطر | الحالة الفعلية |
|---|---|---|
| UN Comtrade (preview + data) | `silk_data_layer.py:40-44, 53-55` | ✅ موصول، يعمل بلا مفتاح (preview)؛ المفتاح يرفع الحد ويحوّل لـ `/data/v1/get` (يثبته اختبار `tests/test_smoke.py:171-182`) |
| World Bank | `silk_data_layer.py:43, 180-205` | ✅ موصول، بلا مفتاح |
| WITS (تعريفات) | `silk_tariffs_agent.py:22` | ✅ موصول، بلا مفتاح، best-effort |
| FAOSTAT | `silk_faostat_agent.py:24` | ✅ موصول، قد يرد بطلب مصادقة فيتدهور بأمان (`:76-80`) |
| Google Trends | `silk_trends_agent.py:32` | ⚠️ موصول لكن **معطّل عملياً في النشر الافتراضي**: حزمة `pytrends` معلَّقة في `requirements.txt:8-9` فلا تُثبَّت |
| Google Places | `silk_maps_agent.py:23` | ⚠️ موصول، معطّل بلا `GOOGLE_MAPS_API_KEY` |
| Serper (بحث ويب) | `silk_websearch_agent.py:24` | ⚠️ موصول، معطّل بلا `SEARCH_API_KEY`؛ المزوّدون الآخرون TODO غير منفّذ (`:40-46`) |
| SerpApi (أسعار محلية) | `silk_localprice_agent.py:34-35` | ⚠️ موصول، معطّل بلا `LOCALPRICE_API_KEY` |
| Volza (مدفوع) | `silk_volza_agent.py:28-29` | ⚠️ موصول دفاعياً، معطّل بلا `VOLZA_API_KEY`؛ نقطة النهاية قابلة للتهيئة لأن مسار الاشتراك غير مؤكد |
| explee (مدفوع) | `silk_explee_agent.py:32-33` | ⚠️ موصول دفاعياً، معطّل بلا `EXPLEE_API_KEY`؛ المخطط (schema) غير مضمون (`:92-95`) |
| Anthropic (كلود) | `silk_ai_judge.py:18-20` | ⚠️ موصول (نموذج `claude-opus-4-8` افتراضياً)، معطّل بلا `ANTHROPIC_API_KEY` |

«موصول» = الكود يستدعي المصدر فعلاً عند توفر شرطه؛ «معطّل» =
يتدهور إلى `DataPoint(None)` موسوم بلا نداء شبكة — وهذا مُختبَر
(`tests/test_smoke.py:153-168, 207-216`).

**تناقض توثيقي يستحق التصحيح:** `.env.example:36-41` ينص حرفياً
«المنصة حالياً لا تستخدم أي نموذج ذكاء اصطناعي... اطلب مني بناءها
لتُفعَّل / not yet built» — بينما الطبقة **مبنية وموصولة فعلاً**
(`silk_ai_judge.py` كاملاً، علم `with_ai` في `api.py:97`، والذكر في
`/sources` `api.py:163`). التعليق متقادم.

---

## ٦) الخلاصة التنفيذية

### ٦.١ جدول الحالة

| البند | الحالة | الدليل |
|---|---|---|
| مبدأ «لا اختلاق» في الوكلاء وطبقة البيانات | ✅ كامل | كل مسارات الفشل ترجع `DataPoint(None, note)` — عينات: `silk_data_layer.py:199-205`، `silk_volza_agent.py:49-50`؛ مفروض بالاختبارات `tests/test_smoke.py:44-53, 153-168` |
| وسم المصدر/الثقة/التاريخ لكل قيمة | ✅ كامل | بنية `DataPoint` — `silk_data_layer.py:58-66` |
| 11 وكيلاً عاملاً بتدهور آمن | ✅ كامل | القسم ١.١ أعلاه |
| **21 وكيلاً** (افتراض الرؤية) | ❌ غائب | 11 فقط؛ العشرة الآخرون غير موجودين (قسم ١.١) |
| ترتيب أسواق موزون شفاف | ✅ كامل | `silk_market_ranker.py:158-232` (أوزان معلنة `:56-61`، إعادة توزين عند النقص `:216-219`) |
| فحوص جودة (وسم لا تعديل) | ✅ كامل | `silk_quality.py:69-94` |
| تخزين التحليلات | ✅ كامل (SQLite) | `silk_storage.py:30-75` |
| **Postgres** | ❌ غائب | القسم ٤.١ |
| **عمود outcome/outcome_date** | ❌ غائب | القسم ٤.٣ (`silk_storage.py:33-38`) |
| حكم لجنة حتمي + حكم كلود | ✅ كامل (بازدواجية) | `silk_agents.py:141-176` + `silk_engine.py:142` — الازدواجية نفسها المرشّحة للتوحيد لاحقاً |
| **حماية الحقن في طبقة كلود** | ❌ غائبة | القسم ٢.٤ (`silk_ai_judge.py:64-94`) |
| **synthesis ثنائية المرحلة** | ❌ غائبة | نداءان مستقلان أحاديا المرحلة (قسم ٢.٤) |
| **مسار `/deepen`** | ❌ غائب | القسم ٢.١؛ المكافئ أعلام `with_*` (`api.py:88-97`) |
| حصر المدفوع خارج المسار الافتراضي | ⚠️ جزئي | أعلام مطفأة + بوابة مفتاح (قسم ١.٣) — لكنه غير بنيوي وقابل للتفعيل من أي طلب عام |
| فشل مرئي في أغلفة الإثراء | ⚠️ جزئي | استثناء الغلاف → `[]`/`None` بلا ملاحظة (جدول القسم ١.٤) |
| **`correlation.py` / بطاقة المنتج** | ❌ غائب | القسم ٢.٢؛ البذرة الوحيدة `compare_own_price` (`silk_localprice_agent.py:145`) |
| **سقف التكلفة** | ❌ غائب | القسم ٥.١ |
| **المصادقة على الـ API** | ❌ غائبة | القسم ٥.١ (`api.py:104-183` كلها عامة) |
| **BaseAgent الفارض بنيوياً** | ❌ غائب | يوجد `Agent(ABC)` بسيط فقط (`silk_agents.py:35-44`) |
| لوحة ويب | ✅ كاملة | `web/index.html` + `api.py:185-190` |
| **تقرير كامل (Word) / مختصر** | ❌ غائبان | القسم ٣.١ |
| تصدير بيانات | ⚠️ جزئي | CSV فقط من اللوحة (`web/index.html:560-565`)؛ JSON متاح عبر الـ API لكن بلا زر تنزيل |
| **قالب عرض موحّد** | ❌ غائب | ثلاثة مسارات عرض مستقلة (قسم ٣.٢) |
| **مجلد `samples/`** | ❌ غائب | القسم ٣.٣ |
| اختبارات دخان هيرمتيكية + CI | ✅ كاملة | 24 اختباراً (`tests/test_smoke.py`)، CI يشغّلها (`.github/workflows/ci.yml:18-19`)؛ ملاحظة: `README.md:179` يقول «10/10» — عدّ متقادم |
| دقة التوثيق (README / .env.example) | ⚠️ جزئية | «9 طبقات» مقابل 11 فعلياً (`README.md:48` مقابل `api.py:152-164`)؛ تعليق AI متقادم (`.env.example:36-41`) |

### ٦.٢ المتبقي — مرتّباً بالأولوية

> **تنبيه منهجي:** لا توجد وثيقة «خطة V3» داخل المستودع
> (**غير موجود** — بحث شامل). القائمة التالية مشتقة من الفجوات بين
> ما تفترضه `docs/VISION.md` (PR ‏#12) عن حالة المنصة وبين الكود
> الفعلي المدقَّق أعلاه، مرتّبةً بالخطورة ثم بالتمهيد لما بعد V3:

1. **المصادقة + سقف التكلفة قبل أول وكيل** — أخطر فجوة تشغيلية:
   `/analyze` عام يستطيع تشغيل طبقات مدفوعة بمفاتيح الخادم
   (قسم ٥.١). شرط مسبق لأي نشر عام بمفاتيح حقيقية.
2. **حماية الحقن في طبقة كلود** — عزل نتائج الوكلاء (نصوص ويب
   وأسماء شركات خارجية) عن جسد البرومبت بآلية بنيوية
   (قسم ٢.٤) — الرؤية تعتبرها قائمة وهي غير موجودة.
3. **توحيد إظهار فشل أغلفة الإثراء** — استبدال `[]`/`None` الصامتة
   بـ `DataPoint(None, note=السبب)` في المواضع التسعة المجدولة
   (قسم ١.٤)، وإزالة افتراض `"WATCH"` (`silk_ai_judge.py:102-104`).
4. **عمود `outcome` + `outcome_date` في جدول `analyses`** —
   تكلفته الآن سطران في `init_db` (`silk_storage.py:33-38`)،
   وقيمته سجل المصداقية التراكمي الذي تبنى عليه أقسام لاحقة
   (٩.٥ و١١.٣ من الرؤية).
5. **قاعدة `samples/` + زر تنزيل JSON خام** — أرخص بندين في قسم
   المخرجات (١٠.٥-١٠.٦ من الرؤية) ولا يعتمدان على شيء.
6. **مسار `/deepen` صريح** بدل أعلام `with_*` المتناثرة — شرط
   مسبق بنيوي لمحرّك التقاطع وخيوطه (القسم الأول من الرؤية)
   ولقرار «المدفوع حصراً ضمن التعميق».
7. **`BaseAgent` الفارض** (تصنيف PAID + الحارس + الفشل البنيوي) —
   يُبنى مع أول وكيل جديد حسب قاعدة الرؤية ٩.٤، والمرشح الطبيعي
   أول وكيل من العشرة الغائبين (قسم ١.١).
8. **مواءمة التوثيق مع الواقع** — إما بناء الوكلاء الغائبين تدريجياً
   أو تصحيح الأرقام: 11 لا 21، «11 طبقة» لا 9 (`README.md:48`)،
   عدّ الاختبارات (`README.md:179`)، وتعليق AI المتقادم
   (`.env.example:36-41`).

---

*انتهى التقرير. أي بند أعلاه قابل للتحقق العكسي بفتح الملف والسطر
المذكورين — ولا يحوي التقرير أي تقييم بلا إسناد.*
