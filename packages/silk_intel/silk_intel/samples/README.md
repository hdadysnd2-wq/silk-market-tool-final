# نماذج المخرجات — Output samples

> قاعدة الموجة ١ (من `docs/EXECUTION_PLAN.md`، درس أزمة الإرفاق في
> `docs/VISION.md` §١٠.٦): كل مخرج يُولَّد منه نموذج فعلي يُحفَظ في
> المستودع نفسه مع كل تعديل على طبقة العرض — المراجع يفتح الملف من
> هنا مباشرة، لا قنوات إرفاق.

| الملف | ما هو | كيف وُلّد |
|---|---|---|
| `report_full_latest.docx` | التقرير الكامل Word (§10.3) من نفس النتيجة — الخلاصة التنفيذية الاحترافية (ai_report) أو الحتمية (exec_summary) + سطر مصدر تحت كل رقم + قسم "الأسواق المرشّحة الأخرى" السردي + «حدود هذا التقرير» | `tools/gen_analyze_samples.py` |
| `brief_latest.txt` | المختصر (§10.4) — صفحة «رسالة جوال» | `tools/gen_analyze_samples.py` |
| `analysis_latest.json` | ردّ `POST /analyze` الحقيقي (تمور × سوقين مبذورين: الصين والإمارات) شاملاً `view` (القالب الموحّد) — نفس شكل مُسلسِل `api.py._to_jsonable` الحقيقي | `tools/gen_analyze_samples.py` |
| `report_full_latest.md` | التقرير الكامل Markdown (Stage 5، §7): قرار الدخول §8 + TAM/SAM/SOM + الأسواق المرشّحة الأخرى + المنافسة بطبقتيها + التسعير بطبقتيه + SWOT + الشرائح + دليل المورّدين + الاشتراطات + سجل المخاطر + ملحق الأثر | `tools/gen_analyze_samples.py` |
| `research_report_latest.docx` | تقرير `POST /research` **الكامل التشغيلي** (للمدقّق — `?internal=1`): غلاف بشارة حكم ملوّنة + التقرير السردي + ملحق التقاطعات + «سجل الأدلة للمدققين» (§6 أمر العمل الرئيس: الحقيقة/المصدر العمومي/الرابط/تاريخ الجمع/قوة الدليل ✓◐○ — بلا رقم ثقة خام، بلا وسم أداة). كامل المستند RTL (§4)، والعملة بالدولار حصراً (§1). المُسلَّم النهائي PDF عبر `GET /analyses/{id}/report.pdf` (§3) | `python3 tools/gen_research_sample.py` (نتيجة إسبانيا×تمور مموّهة) |
| `research_report_latest.md` | مخرَج `GET /analyses/{id}/report.md` لنتيجة **بحث عميق** — يُصيَّر من `deep_research` (السرد المراجَع + الحكم + ملحق الأدلة + أثر المصادر + الحدود)، لا من قالب /analyze الفارغ (تدقيق تمور/هولندا: كان يُخرِج «تغطية 0.0%»/«with_research غير مفعّلة»؛ الترويسة الآن صادقة — «ثقة التصنيف —» لا "None") | `python3 tools/gen_research_sample.py` (نتيجة إسبانيا×تمور مموّهة) |
| `client_report_latest.docx` | **تقرير العميل** (القالب الثاني، فصل الجمهور — الافتراضي على `GET /analyses/{id}/report.docx` لنتائج البحث العميق): بنية استشارية بسبعة أقسام (القرار وأساسه → السوق بالأرقام → المنافسة والتسعير → مسار الدخول والمتطلبات → المخاطر → ما لم يكتمل للقرار → المنهجية وسجل الأدلة للمدققين). **صفر تِلِمِتري**: لا بعثات/حالات/أسماء أدوات/لغة خوارزمية — حارس نصّي يرفض التصدير إن تسرّب أيٌّ منها. الفجوات تُصاغ تجارياً («لم نتمكّن من توثيق X — إغلاق هذه الفجوة يتطلّب Y»)، وجدول البعثات يُستبدَل بفقرة منهجية ٣ أسطر | `python3 tools/gen_client_report_sample.py` (نتيجة هولندا×تمور مموّهة موسومة «نموذج توضيحي») |

**ملاحظة صدق:** عيّنات `/analyze` الأربع أعلاه (باستثناء `research_report_
latest.docx`) وُلّدت معاً من تشغيلة `silk_engine.analyze` حتمية واحدة
(محرّك حقيقي + مخزن حقائق مبذور بسوقين: الصين والإمارات) داخل حاجز شبكة
صادق — لا اختلاق؛ فشل الجلب الحي (Comtrade/World Bank/FAOSTAT) مُعلَن
كفجوة في التقرير نفسه لا مُخفى. أعد توليدها بعد أي تعديل على طبقة العرض
(قاعدة §10.6):

```bash
python3 tools/gen_analyze_samples.py
```

لتوليد نموذج ببيانات حية بدل المبذورة، شغّل نفس السكربت من بيئة متصلة
بالشبكة (النشر) — الحاجز الحتمي داخل السكربت يبقى، فالنتيجة ستقرأ من
المخزن أولاً كما في الإنتاج؛ لتحليل حي كامل استخدم `silk_engine.analyze`
مباشرة بلا حاجز الشبكة.
