# BASELINE — 2026-07-16 (FROZEN)

> **Decision D-04.** This file is measured once, in Command #1, and frozen.
> Every before/after in Commands #2–#6 compares against **this file**, never a
> moving target. Part E re-measures into `docs/BASELINE-post-BC.md` because B and
> C legitimately shift the baseline.
>
> **Provenance discipline (Lesson 1 / Lesson 10).** Each number below is labeled
> by how it was obtained: `[repo-measured]` = reproduced here from the repo in
> this read-only session; `[owner-verified]` = reported by the owner in
> `docs/SPEC-v2.md` (owner-side live measurement, not re-run here — no `usage.db`
> ships in the repo, so cost/runtime cannot be independently reproduced offline).
> Nothing here is guessed.

Frozen at commit `f85e40d` (last code commit, PR #106; the docs-only commit that
added SPEC-v2 + DECISIONS + this file changes no product code, so all
`[repo-measured]` facts below hold at both SHAs).

---

## 1. Test suite — `[repo-measured]`

```
python3 -m pytest tests/ -q
→ 1045 passed, 2 skipped, 1 warning in ~89s
```

Full hermetic suite green. This is the regression floor: every later command must
keep this green and add its own lock-tests on top.

---

## 2. Cost & runtime (live `/research` run) — `[owner-verified]`

| Metric | Baseline | Target | Source |
|---|---|---|---|
| Cost per run | **~$2.0/run** | ≤ $1.5 (measured only after Part E, D-01 interim ceiling $1.7) | `docs/SPEC-v2.md:33` |
| Runtime per run | **~20 min** | < 10 min | `docs/SPEC-v2.md:34` |

No `usage.db` / `silk.db` is present in the repo (fresh clone ships only seed CSVs
under `data/`), so these two numbers cannot be reproduced offline — they are the
owner's live-side measurements and are frozen here as-reported. Part E must print
its own live cost/runtime from `usage.db` / `llm_usage` to close against these.

**Cost-shape facts that ARE repo-measured** (relevant to Part E):
- Review loop default: **up to 2 cycles**, cycle 2 fires on *any* issue, not
  blocking-only (`silk_ai_judge.py:1127,1162,1165,1169`). `SILK_MAX_REVIEW_CYCLES`
  does **not** exist in code (grep-confirmed empty; proposed only in
  `docs/SPEC-v2.md:133`).
- Model routing: **Opus (`claude-opus-4-8`) for all 12 missions + analyst +
  writer**; Haiku (`claude-haiku-4-5`) only for reviewer + small classification
  helpers (`silk_ai_judge.py:20,111,1111`; `silk_llm_runtime.py:922,990`). No
  Sonnet tier anywhere; no Haiku-for-extraction routing.
- Prompt caching **on** (`cache_control: ephemeral`) for shared context
  (`silk_llm_provider.py:132,174,182`; `silk_llm_runtime.py:789-809`).
- Cost is metered: per-model + per-mission via `silk_pricing`/`silk_context`
  (`api.py:919-929`). **No per-tail-stage** line item (analyst/writer/reviewer run
  outside `mission_context`, land only in the global aggregate —
  `silk_context.py:194`).
- Concurrency: 11 core missions run in a ThreadPoolExecutor
  (`silk_missions.py:437`), mission 12 sequential after them
  (`silk_missions.py:474-480`); the analyst→synthesis→writer→reviewer **tail is
  strictly sequential** (`api.py:884-898`). No asyncio.
- Retry loops are all bounded — no storms (`silk_ai_judge.py:37,995`;
  `silk_llm_runtime.py:886,901`).
- Timing: per-LLM-call `elapsed_ms` in traces + one run-level `elapsed_seconds`
  in the progress UI (`api.py:1237-1257`). **No per-stage wall-time rollup.**

---

## 3. UI state — `[repo-measured]` (`web/index.html`)

Three action buttons in the runbar (`web/index.html:248-253`), DOM order:
1. `#snapBtn` — "معاينة فورية" (`:250`) — **should be deleted** (A1); makes it three.
2. `#researchBtn` — "بحث عميق (كلود) ←" (`:251`) — intended primary.
3. `#runBtn` — "مسح الأسواق ←" (`:252`) — intended secondary.

History sidebar present and server-backed (`#histList` `web/index.html:202`;
`loadServerHistory()` `:565`, `drawHist()` `:587`).

Orphan strings still live:
- snapshot feature fully live: `#snapBtn`, `snapOut`, `quickSnapshot`,
  `/products/snapshot` route (`api.py:1675`), `silk_snapshot.py` module,
  `save/get_product_snapshot` (`silk_storage.py:463,487`).
- "حلّل السوق" leftovers: `silk_market_analyst.py:162` (docstring),
  `web/index.html:407` (stale comment), `docs/mockups/silk_prototype.html:273`.

---

## 4. Deep-research report content — `[repo-measured]` (`samples/research_report_latest.md`, masqueraded fixture)

> The committed sample is a masqueraded (مموّه) Netherlands/Spain fixture, not a
> live run — so it shows the *structural* baseline, not the owner's live-run
> defects. Live-only defects are labeled below from code paths + the SPEC's
> owner-verified table.

| Property | Baseline value |
|---|---|
| Size | 195 lines / 502 words |
| `## ` sections | 16 |
| Glossary (مسرد المصطلحات) | **absent** (grep count 0) — B1 gap |
| Standalone jargon at first use | **HHI ×6, LPI ×1** unglossed — B1/B3 gap |
| USD→SAR contextualization (ريال) | **absent** (count 0) — B1 gap |
| Contactable-importers table (قائمة مستوردين وموزعين قابلين للتواصل) | **absent** (count 0) — C5 gap |
| Email/phone lead columns | **absent** — C2/C5 gap |
| §9 WGI | qualitative sentence only, **no numeric PV.EST/RL.EST, no جودة التنظيم** (`samples/research_report_latest.md:111`) — D3 gap |

**Assembly-defect code sites frozen for Part D before/after:**
- D1 limits: assembled by pure concatenation, **no reconciliation** vs final fact
  store (`silk_render.py:912-925`). Truncation with `"…"` at **two** sites —
  assembly/storage (`silk_llm_runtime.py:648-658` via
  `silk_market_analyst.py:140,215`) and render (`silk_reports.py:81-92`); both
  retreat to a word boundary (mid-word bug already fixed) but still append `"…"`.
- D2 intersections: category normalization fixed
  (`silk_market_analyst.py:197-205`), but classification depends on Claude
  bracket-tagging each note (`:198-204`) with **no fallback classifier** →
  residual fact-loss risk; `"تقاطع المحلل بلا أدلة كافية"` still emitted
  (`silk_render.py:923`).
- D3 §9: WGI **fetch** path fixed + lock-tested (`silk_data_layer.py:382-385,
  412-444`; `tests/test_technical_mission_failures_item2.py:39,58,76`), but §9 has
  **no deterministic binding** of stored WGI facts — the writer relies on the
  `risk_news` mission surfacing them (`silk_ai_judge.py:918-921`). Writer-mapping
  gap, not a fetch bug.

---

## 5. `google_maps` source — `[repo-measured]`

**Configured-but-unused in the `/research` path.** `/health` shows it "on" purely
on key presence (`api.py:315-317`), but no `/research` mission has a maps/places
tool — the full mission tool vocabulary (`silk_missions.py` `allowed_tools`;
runtime registry `silk_llm_runtime.py:143-406`) contains no `find_places`. It is
only exercised by the OLD `/analyze` path (`silk_engine.py:164-165,470-471`
enrichment; `silk_research.py:395,650`). Confirmed by `docs/PLATFORM_ANALYSIS.md:173`.

---

## What "changed the baseline legitimately" means (D-04)

- Commands #2–#4 (UI, assembly, language) change §3/§4 above → compared against
  THIS file.
- Commands #5–#6 (scraper, cost/speed) change §2/§4/§5 → Part E re-freezes into
  `docs/BASELINE-post-BC.md`, and the ≤$1.5 / <10min targets are measured against
  that post-B/C baseline, per D-01 and the ordering note in `docs/DECISIONS.md`.
